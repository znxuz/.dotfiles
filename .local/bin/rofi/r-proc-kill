#!/usr/bin/env bash

pid="$(ps -U "$(whoami)" | awk '{print $NF}' | sort | uniq | rofi -dmenu -i -sep'\n' -p 'kill')"
[[ -n "$pid" ]] && killall "$pid" > /dev/null 2>&1 && notify-send -u low " $pid"
