#!/usr/bin/env bash

aur_helper=yay

pac_upd()
{
	[[ -f /var/lib/pacman/db.lck ]] && sudo rm /var/lib/pacman/db.lck

	$aur_helper -Syu --noconfirm && pgrep -f 'poly-pkg-upgrade' &> /dev/null &&
		poly-pkg-upgrade -u

	return 0
}

pac_upd
