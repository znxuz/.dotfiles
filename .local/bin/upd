#!/usr/bin/env bash

pac_upg()
{
    sudo pacman -Syu --noconfirm && paru -Syu --noconfirm && pgrep -f 'poly-pkg-upgrade' &> /dev/null && poly-pkg-upgrade -u
}

nvim_upg()
{
    nvim +'PackerSync'
}

pac_upg; nvim_upg
