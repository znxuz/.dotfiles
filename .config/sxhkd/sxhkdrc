# volume control
shift + alt + {F10, F11}
    i3-volume {down, up} 5 -np -N dunstify
# brightness control
shift + alt + {F1, F2}
    xbacklight {-dec 20 && dunstify -r 1 -u low {"Brightness down", "Brightness up"}
# alt + F2
    # xbacklight -inc 20 && dunstify -r 1 -u low "Brightness up"
